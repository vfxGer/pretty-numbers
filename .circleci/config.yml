jobs:
  test:
    executor: python/default
    steps:
      - checkout
      - run:
          command: |
            pip install tox && tox
          name: Test it
orbs:
  python: circleci/python@1.0.0
version: 2.1
workflows:
  main:
    jobs:
      - test
